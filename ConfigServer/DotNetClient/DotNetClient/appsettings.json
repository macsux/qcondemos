{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "spring": {
    "application": {
      "name": "HelloWorld"
    },
    "cloud": {
      "config": {
        // uncomment next line to target config settings stored in git "release" branch
        //"label": "release"
        // uncomment this line to force use of "dev" config profile. alternatively set ASPNETCORE_ENVIRONMENT to "dev" 
        //"env": "dev"
      }
    }
  }
}
